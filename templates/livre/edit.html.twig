{% extends 'base.html.twig' %}

{% block title %}Modifier Infos Livre{% endblock %}

{% block body %}
    <h1>Modifier Infos Livre</h1>

    {{ include('livre/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <div>
        <a href="{{ path('livre_index') }}">Retour à la liste</a>
    </div>
    {{ include('livre/_delete_form.html.twig') }}
{% endblock %}